import NavBar from '../utils/NavBar';

const ReadingList = () => {
    return (
      <main>
        <NavBar selected='books'/>
        <p className='pl-2'>No shot I'm porting this over buy here's my Notion page</p>
      </main>
    )
};

export default ReadingList;